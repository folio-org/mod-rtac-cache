{
  "type": "object",
  "description": "Real Time Availability Check (RTAC) holding summary details",
  "additionalProperties": false,
  "properties": {
    "instanceId": {
      "type": "string",
      "description": "The FOLIO instance id"
    },
    "hasVolumes": {
      "type": "boolean",
      "description": "Indicates if the holding has volume information"
    },
    "locationAvailability": {
      "type": "array",
      "description": "A summary of all availability summary statuses organized by library and location",
      "items": {
        "type": "object",
        "$ref": "locationAvailability.json"
      }
    }
  },
  "required": [
    "instanceId",
    "hasVolumes"
  ]
}
