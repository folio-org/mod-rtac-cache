{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "Piece resource event",
  "type": "object",
  "properties": {
    "id": {
      "description": "UUID of the event",
      "type": "string"
    },
    "action": {
      "description": "Action performed on the piece",
      "$ref": "pieceEventAction.json"
    },
    "pieceId": {
      "description": "UUID of the piece",
      "type": "string"
    },
    "userId": {
      "description": "UUID of the user who performed the action",
      "type": "string"
    },
    "eventDate": {
      "description": "Date time when event triggered",
      "format": "date-time",
      "type": "string"
    },
    "actionDate": {
      "description": "Date time when piece action occurred",
      "format": "date-time",
      "type": "string"
    },
    "pieceSnapshot": {
      "description": "Snapshot of the piece at the time of the event",
      "$ref": "../orders/piece.json"
    }
  },
  "additionalProperties": false
}
