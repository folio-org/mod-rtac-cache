{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "description": "Records the relationship between a part of a bound-with (a holdings-record) and the bound-with as a whole (the circulatable item)",
  "type": "object",
  "properties": {

    "id": {
      "$ref": "../uuid.json"
    },
    "instanceId" : {
      "description": "UUID of the instance",
      "$ref": "../uuid.json"
    },
    "holdingsRecordId": {
      "description": "UUID of the holdings record that is part of the bound-with",
      "$ref": "../uuid.json"
    },
    "itemId" : {
      "description": "UUID of the bound-with item",
      "$ref": "../uuid.json"
    },
    "metadata": {
      "$ref": "../metadata.json"
    }
  },
  "additionalProperties": false,
  "required": [
    "holdingsRecordId",
    "itemId"
  ]
}
